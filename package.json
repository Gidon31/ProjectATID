{
  "name": "projectatid",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test --reporter=html && node decorate-report.js && playwright show-report",
    "ai-agent": "node ai-playwright-agent.js",
    "ai-agent:guest": "node ai-playwright-agent.js tests/guest-checkout.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "allure-playwright": "^2.7.0"
  }
}
